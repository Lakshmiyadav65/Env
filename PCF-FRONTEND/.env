# URL opened in a new tab when "Browse Guides" is clicked in the sidebar.
# Points at the deployed Knowledge Base (Vercel) so it works without a local server.
# Use the app root (no path) so it lands on the Knowledge Base dashboard.
# For local KB dev instead, set this to http://localhost:5174 (npm run dev in ../KNOWLEDGE-BASE).
VITE_KNOWLEDGE_BASE_URL=https://enviraan-knowledge-base.vercel.app
