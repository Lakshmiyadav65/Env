# URL of the standalone Knowledge Base app (opened in a new tab from the sidebar).
# Local dev: the KB runs on port 5174 (npm run dev inside ../KNOWLEDGE-BASE).
# Production: set this to the deployed Knowledge Base URL.
VITE_KNOWLEDGE_BASE_URL=http://localhost:5174
