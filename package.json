{
  "name": "enviraan",
  "private": true,
  "version": "1.0.0",
  "description": "Enviraan workspace - the PCF Supplier Intelligence Suite (includes the merged Knowledge Base)",
  "scripts": {
    "dev": "npm --prefix PCF-FRONTEND run dev",
    "build": "npm --prefix PCF-FRONTEND run build",
    "lint": "npm --prefix PCF-FRONTEND run lint",
    "preview": "npm --prefix PCF-FRONTEND run preview",
    "install:all": "npm install && npm --prefix PCF-FRONTEND install"
  }
}
